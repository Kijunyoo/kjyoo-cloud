# sync-notion.mjs 설정. 이 파일을 notion.env 로 복사해 값을 채운다.
# notion.env 는 커밋하지 않는다(.gitignore 대상). 실제 토큰은
# 로컬 자격증명 볼트의 Notion API 항 참조.

NOTION_TOKEN=ntn_로 시작하는 인테그레이션 토큰
NOTION_PAGE_ID=편집 페이지의 32자리 ID (하이픈 포함/미포함 둘 다 허용)
